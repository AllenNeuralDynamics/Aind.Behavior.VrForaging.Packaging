# Example: query local export

Query the export output on disk with pandas and DuckDB.

Run with uv (dependencies declared inline via PEP 723 — no project install needed):

```bash
uv run docs/examples/query_export.py
```

---

```python
--8<-- "docs/examples/query_export.py"
```
