# cli

The `vr-foraging-packaging` command. One subcommand per pipeline function:
`session`, `batch`, `aggregate`.

---

::: aind_behavior_vr_foraging_packaging.pipeline.cli
    options:
      members:
        - SessionCommand
        - BatchCommand
        - AggregateCommand
        - Cli
